server:
  port: 8081

spring:
  application:
    name: AUTH-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/parkease_auth
    username: root
    password: ${DB_PASSWORD:8770B26@k}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    instance-id: ${spring.application.name}:${server.port}
    prefer-ip-address: true
    ip-address: 127.0.0.1
    hostname: localhost

  cloud:
    inetutils:
      preferred-networks:
        - 127.0.0.1

google:
  client:
    id: 890321455440-m23ejitqeu8pift9ni92jmv6cf8220l0.apps.googleusercontent.com

jwt:
  secret: P@rkEase_Super_S3cur3_JWT_K3y_2026_!@#$